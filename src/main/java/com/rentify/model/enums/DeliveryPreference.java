package com.rentify.model.enums;

/**
 * How a renter would like to receive (or hand back) the item / service on a rent wish post.
 */
public enum DeliveryPreference {
    PICKUP,
    DELIVERY,
    EITHER
}
